# 🔐 RabbitBotSend - Система верификации пользователей

Современная система верификации пользователей через Telegram бота с использованием асинхронной архитектуры, брокера сообщений и REST API.

## 📋 Описание

Проект представляет собой комплексное решение для верификации пользователей, включающее:
- **FastAPI** сервер для обработки запросов
- **Telegram бота** для отправки кодов подтверждения  
- **Брокер сообщений** (RabbitMQ) для асинхронной коммуникации
- **SQLite** базу данных для хранения пользователей и кодов
- **Генерацию уникальных кодов** подтверждения

## ✨ Возможности

- 🔢 Генерация уникальных 7-значных кодов подтверждения
- 🤖 Автоматическая отправка кодов через Telegram бота
- ⚡ Асинхронная обработка запросов через брокер сообщений
- 🔍 Верификация пользователей по кодам
- 📊 Диагностические эндпоинты для мониторинга БД
- 🛡️ Валидация данных через Pydantic схемы
- 🚀 Готовность к production развертыванию

## 🛠️ Технологии

### Backend
- **FastAPI** (≥0.121.3) - Современный веб-фреймворк для Python
- **SQLAlchemy** (≥2.0.44) - ORM для работы с базой данных
- **Aiogram** (≥3.22.0) - Фреймворк для создания Telegram ботов
- **FastStream** (≥0.6.3) - Фреймворк для работы с брокерами сообщений
- **Uvicorn** (≥0.38.0) - ASGI сервер для запуска приложения

### База данных
- **SQLite** с aiosqlite (≥0.21.0) - Легковесная встраиваемая БД
- Асинхронные сессии SQLAlchemy
- Автоматическая миграция схемы БД

### Брокер сообщений
- **RabbitMQ** - Надежный брокер сообщений
- Асинхронная коммуникация между API и ботом

## 📁 Структура проекта

```
📦 RabbitEmailSend/
├── 📄 main.py                    # Точка входа приложения
├── 📄 bot.py                     # Telegram бот
├── 📄 pyproject.toml             # Конфигурация проекта и зависимости
├── 📄 README.md                  # Документация проекта
├── 📁 api/                       # API компоненты
│   ├── 📄 broker.py              # Инициализация брокера сообщений
│   └── 📁 router/                # Роутеры API
│       ├── 📄 router.py          # Основные роуты аутентификации
│       └── 📄 db_router.py       # Диагностические роуты БД
├── 📁 client/                    # Клиентские компоненты
│   └── 📄 lifespan.py            # Обработчик жизненного цикла
├── 📁 db/                        # Компоненты базы данных
│   ├── 📄 base.py                # Базовые модели SQLAlchemy
│   ├── 📄 engine.py              # Настройка подключения к БД
│   ├── 📄 models.py              # Модели данных
│   └── 📄 session.py             # Управление сессиями БД
├── 📁 function/                  # Бизнес-логика
│   └── 📄 generate_code.py       # Генерация уникальных кодов
└── 📁 schemas/                   # Схемы валидации данных
    └── 📄 schema.py              # Pydantic схемы
```

## 🚀 Установка и запуск

### Требования
- Python ≥ 3.13
- RabbitMQ сервер
- Telegram Bot Token



## 📡 API Документация

После запуска API сервера документация доступна по адресам:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Основные эндпоинты

#### Отправка кода верификации
```http
POST /auth/send_code
Content-Type: application/json

{
  "username": "user123"
}
```

**Ответ:**
```json
{
  "status_code": 200,
  "message": "Сообщение было отправлено"
}
```

#### Проверка кода
```http
POST /auth/verify_code
Content-Type: application/json

{
  "username": "user123",
  "code": 1234567
}
```

**Ответ:**
```json
{
  "status_code": 200,
  "message": "Успешная регистрация"
}
```

### Диагностические эндпоинты

#### Получение всех пользователей
```http
GET /db/users
```

#### Получение всех верифицированных пользователей
```http
GET /db/pendings
```

## 🗄️ База данных

### Модели данных

#### User (Пользователи для верификации)
- `username` (str, primary_key) - Имя пользователя
- `code` (int, indexed) - Код подтверждения

#### PendingUser (Верифицированные пользователи)
- `id` (int, primary_key) - Уникальный идентификатор
- `username` (str, indexed) - Имя пользователя
- `status` (str) - Статус пользователя (по умолчанию: "user")

### Инициализация БД
База данных автоматически инициализируется при запуске приложения. Таблицы создаются на основе SQLAlchemy моделей.

## 🤖 Telegram Бот

### Функциональность бота
- **Автоматическая отправка кодов** подтверждения пользователям
- **Уведомления об успешной верификации** 
- **Обработка ошибок** с информативными сообщениями

### Подписки на очереди
- `code` - Отправка кодов подтверждения
- `success` - Уведомления об успешной верификации

## 🔄 Архитектура системы

### Поток верификации
1. **Пользователь** отправляет запрос на `/auth/send_code`
2. **API** генерирует уникальный код и сохраняет в БД
3. **Брокер** передает сообщение в очередь `code`
4. **Бот** получает сообщение и отправляет код пользователю
5. **Пользователь** отправляет код на `/auth/verify_code`
6. **API** проверяет код, создает PendingUser, удаляет старого пользователя
7. **Брокер** передает сообщение в очередь `success`
8. **Бот** отправляет уведомление об успехе

### Асинхронная коммуникация
- FastAPI обрабатывает HTTP запросы
- FastStream обеспечивает асинхронную коммуникацию через RabbitMQ
- Бот работает в отдельном процессе и реагирует на сообщения из очередей

## 🛡️ Безопасность

- ✅ Валидация входных данных через Pydantic схемы
- ✅ Использование первичных ключей для предотвращения дублирования
- ✅ Проверка уникальности кодов перед сохранением
- ✅ Закрытие соединений с БД после использования
- ✅ Обработка ошибок с информативными сообщениями

## 🔧 Конфигурация

Основные настройки конфигурируются через файл `config.py`:
- Подключение к базе данных
- Telegram Bot Token  
- Настройки брокера сообщений
- Названия таблиц в БД

## 📊 Мониторинг

### Логи приложения
Приложение выводит логи с информацией о:
- Запуске и остановке сервисов
- Обработке сообщений брокером
- Ошибках подключения к БД
- Статусе верификации пользователей

### Диагностические эндпоинты
- `/db/users` - просмотр всех пользователей в системе верификации
- `/db/pendings` - просмотр всех верифицированных пользователей

## 🤝 Разработка

### Локальная разработка
1. Установите зависимости для разработки
2. Запустите RabbitMQ локально
3. Настройте конфигурацию в `config.py`
4. Запустите API сервер с hot reload: `uvicorn main:app --reload`
5. Запустите бота: `python bot.py`

### Добавление новых функций
- Роуты добавляются в соответствующие файлы в `api/router/`
- Бизнес-логика размещается в папке `function/`
- Модели данных определяются в `db/models.py`
- Схемы валидации создаются в `schemas/`


